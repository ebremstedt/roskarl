# 3.1.27 — 2026-05-25

## Bug fixes

- **`env_var_dsn`** now URL-decodes the database name when parsing a DSN, so database names with URL-encoded characters (e.g. spaces written as `%20`) round-trip correctly. ([#13](https://github.com/ebremstedt/roskarl/pull/13) by @axelronngren)
